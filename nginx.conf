server {
    listen 80;
    server_name localhost;

    # Gzip settings
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # Static files - Frontend build
    location / {
        root /usr/share/nginx/html;
        index index.html;
        # Try finding the exact file. If not found, proxy to backend
        # to handle route and inject SEO tags (instead of just falling back to static /index.html)
        try_files $uri $uri/ @proxy_to_backend;
    }

    location = /sitemap.xml {
        root /usr/share/nginx/html;
        add_header X-Robots-Tag "index, follow";
    }

    location = /robots.txt {
        root /usr/share/nginx/html;
        add_header X-Robots-Tag "index, follow";
    }

    location @proxy_to_backend {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # NEVER cache critical PWA entry points: index.html, service workers, manifest
    location ~* ^/(index\.html|sw\.js|manifest\.webmanifest|sitemap\.xml|robots\.txt)$|^/workbox-.*\.js$ {
        root /usr/share/nginx/html;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Pragma "no-cache";
        add_header Expires "0";
    }

    # Enable heavy caching ONLY for static, hashed assets (everything else)
    location ~* \.(?:css|js|jpg|svg|png|webp|woff2?|eot|ttf|ico)$ {
        root /usr/share/nginx/html;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Backend API
    location /api/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Public results for OG tags (handled by backend)
    location /results/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
